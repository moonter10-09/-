<!--pages/detail-video/index.wxml-->
<view class="main">
  <video class="video" src="{{mvUrlInfo.url}}"
    autoplay="true"
    loop="true"
    show-mute-btn="true"
    danmu-list="{{danmuList}}"
    ></video>
</view>
<!-- wx:if="{{item.urgent=='1'}}  wx:if="{{item.transferflag=='1'}}"-->